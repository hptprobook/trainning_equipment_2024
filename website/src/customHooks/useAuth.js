// import { useSelector } from 'react-redux';

const useAuth = () => {
  return true;
  // const tokenGit = useSelector((state) => state.auth.tokenGit);
  // const userGit = useSelector((state) => state.auth.userGit);

  // return tokenGit && userGit;
};

export default useAuth;
